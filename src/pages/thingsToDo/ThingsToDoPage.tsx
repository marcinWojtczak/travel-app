import { useParams } from "react-router-dom"

export default function ThingsToDoPage() {
  const { title } = useParams()

  
  return (
    <div className='absolute top-[83px] h-[2000px]'>
      
    </div>
  )
}
